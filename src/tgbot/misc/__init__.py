"""
The misc package contains other auxiliary modules necessary for the bot's work.

Package structure:
    bot_commands.py     - the module contains functions that change the default list of commands for the bot
    filters.py          - the module contains various filters used in handlers
    fsm_states.py       - states for the FSM (Final State Machine)
    keyboards.py        - keyboards used by the bot
    middlewares.py      - middlewares used by the bot
"""
